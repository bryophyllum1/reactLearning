//import statements
// built-in module
// 3rd party module
// custom made module - only for these we provide relative path otherwise we provide just name

// import {add, sub, mul} from "./math-lib"

import mul, {add, sub} from "./math-lib";

add(2, 3);
sub(4, 5);
mul(1, 0);